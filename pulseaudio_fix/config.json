{
  "name": "Alsa & PulseAudio Fix",
  "version": "3.0",
  "slug": "pulseaudio_fix",
  "description": "This add-in eliminates the problem of 'Device or resource busy' on computers with an external ALSA and PULSEAUDIO caused by the docker container 'hassio_audio'",
  "arch": ["armhf", "armv7", "aarch64", "amd64", "i386"],
  "startup": "application",
  "boot": "auto",
  "audio": true,
  "options": {},
  "schema": {}
}
